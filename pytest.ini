[pytest]
addopts = -ra --strict-markers
testpaths = tests
markers =
    ui: marks UI browser tests
    smoke: marks smoke test coverage
    regression: marks regression coverage
    data_driven: marks Excel-driven tests
    report_demo: marks intentional failure demo for report screenshot verification

